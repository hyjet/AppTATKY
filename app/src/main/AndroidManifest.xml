<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kevinli5506.appta">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.NotificationDetailPage"></activity>
        <activity android:name=".View.NotificationListPage" />
        <activity android:name=".ForgetPasswordPage" />
        <activity android:name=".View.OrderListDetailPage" />

        <activity android:name=".View.LaunchPage" />
        <activity android:name=".View.SignUpPage" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".View.EventDetailPage" />
        <activity android:name=".View.EventPage" />
        <activity android:name=".View.HistoryDetailPage" />
        <activity android:name=".View.HistoryPage" />
        <activity android:name=".View.WithdrawPage" />
        <activity android:name=".View.RewardPage" />
        <activity android:name=".View.NewsDetailPage" />
        <activity android:name=".View.NewsRatingActivity" />
        <activity android:name=".View.DonationPage" />
        <activity android:name=".View.HomePage" />
        <activity android:name=".View.EditProfilePage" />
        <activity android:name=".View.RewardDetailPage" />
        <activity android:name=".LocationPickerActivity" />
        <activity android:name=".View.CalculatePage" />
        <activity android:name=".View.LoginPage" />
        <activity android:name=".MainEmptyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>